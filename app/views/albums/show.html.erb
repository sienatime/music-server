<h1><%= @album.title %></h1>
<h2>by <%= link_to @album.artist.name, artist_path(@album.artist) %></h2>

<table>
<thead>
  <tr>
    <td>Track</td>
    <td>Title</td>
    <td>Length</td>
  </tr>
</thead>
<tbody>
<% @album.songs.order(track_number: :asc).each do |song| %>
<tr>
  <td><%= song.track_number%></td>
  <td><%= song.title %></td>
  <td><%= format_song_length(song.length) %></td>
</tr>

<% end %>
</tbody>
</table>
